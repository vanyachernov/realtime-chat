<Window x:Class="RealtimeChat.Client.Wpf.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Join Chat" Height="200" Width="340"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1a1a2e">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="24" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Enter your username"
                   Foreground="#e2e2e2"
                   FontSize="16"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center" />

        <TextBox Grid.Row="2"
                 x:Name="UsernameBox"
                 FontSize="14"
                 Foreground="#e2e2e2"
                 Background="#0f3460"
                 BorderBrush="#533483"
                 BorderThickness="1"
                 Padding="10,8"
                 CaretBrush="#e94560"
                 MaxLength="100" />

        <Button Grid.Row="4"
                Content="Join"
                Click="JoinButton_Click"
                FontSize="14"
                Height="36"
                Foreground="#e2e2e2"
                Cursor="Hand"
                BorderThickness="0">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Bd"
                                        Background="#e94560"
                                        CornerRadius="6"
                                        Padding="0,6">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#c03050" />
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#a02040" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
